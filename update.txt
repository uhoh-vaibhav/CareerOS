I want you to perform a full **stability, data synchronization, and real-AI integration audit** of my existing CareerOS project.

The project already has many implemented features, but there are multiple bugs and inconsistencies.

The biggest issue is:

**Data is not synchronized properly across modules, so AI analysis is often working with stale, incomplete, duplicated, or mock data.**

I want to fix this at the architecture level.

Do NOT rebuild the whole project from scratch.

Do NOT create duplicate versions of existing features.

Do NOT keep temporary mock behavior in production paths.

The main objective is to make CareerOS operate as one connected system where all modules use the latest real user data and real LLM-generated analysis.

---

# PRIMARY GOALS

Fix CareerOS so this data flow works correctly:

User Profile
↓
Latest Resume
↓
Resume Analysis
↓
Detected Skills
↓
Target Role
↓
Skill Gap Analysis
↓
Learning Roadmap
↓
Roadmap Milestones
↓
Study Material
↓
AI Mentor Context
↓
Mock Interview Context
↓
Interview Evaluation
↓
Career Readiness Score
↓
Overview Dashboard

Every module should use the latest persisted data.

No module should rely on unrelated hardcoded/mock values.

---

# 1. FULL PROJECT AUDIT FIRST

Before modifying anything, inspect the complete repository.

Identify:

* frontend
* backend
* AI service
* database schema
* API contracts
* DTO/Pydantic models
* TypeScript interfaces
* LLM provider/client
* prompt files
* data fetching hooks
* shared state
* caching
* mock data
* fallback data
* static analysis responses
* duplicated business logic
* duplicated scoring logic
* duplicated skill data
* outdated endpoints
* stale response shapes

Do not immediately start rewriting code.

Create an internal dependency/data-flow map first.

---

# 2. IDENTIFY THE SOURCE OF TRUTH

Define a clear source of truth for every important CareerOS entity.

At minimum inspect and define ownership for:

User

Latest Resume

Resume Parsed Content

Resume Analysis

Detected Skills

Target Role

Skill Gap Analysis

Roadmap

Roadmap Milestones

Study Material

AI Mentor Conversations

Mock Interviews

Interview Feedback

Portfolio

Career Readiness

Certificates

The database/backend should be the source of truth for persistent user state.

The frontend should NOT maintain separate permanent versions of the same data.

---

# 3. FIX THE LATEST RESUME FLOW

CareerOS must know exactly which resume is the active/latest resume.

When the user uploads a new resume:

1. save resume metadata
2. extract/parse content
3. run real AI resume analysis
4. persist analysis
5. persist detected skills
6. mark this resume as active/latest
7. invalidate dependent stale analyses

Dependent data may include:

* skill gap
* roadmap recommendations
* mentor context
* interview personalization
* readiness score

Do not silently keep old analysis tied to a previous resume.

---

# 4. ADD DATA VERSIONING / DEPENDENCY TRACKING

A major cause of stale analysis is that CareerOS cannot tell whether downstream data was generated from the latest upstream data.

Add a clean way to track this.

For example, analyses may store:

resumeId

targetRoleId or targetRole

generatedAt

sourceVersion / updatedAt

Do not over-engineer this.

But CareerOS must be able to answer:

"Was this skill-gap analysis generated from the current resume?"

"Was this roadmap generated from the latest skill-gap analysis?"

"Was this readiness score calculated using the latest interview/portfolio/resume data?"

If not current, mark the data stale and regenerate or ask the user to refresh.

---

# 5. DEPENDENCY CHAIN

Use a dependency model similar to:

Resume changes
→ Resume Analysis stale
→ Skill Gap stale
→ Roadmap potentially stale
→ Mentor context changes
→ Interview personalization changes
→ Readiness stale

Target Role changes
→ Skill Gap stale
→ Roadmap stale
→ Interview personalization stale
→ Readiness stale

Skill Gap changes
→ Roadmap stale
→ Mentor context changes
→ Readiness stale

Roadmap progress changes
→ Mentor context changes
→ Readiness may change if roadmap contributes

Interview completed
→ Interview score updates
→ Readiness stale

Portfolio changes
→ Portfolio score updates
→ Readiness stale

Implement this safely.

Do not regenerate expensive AI results on every page render.

---

# 6. REMOVE ALL PRODUCTION MOCK DATA

Search the entire repository for:

mock
dummy
sample
fake
placeholder
hardcoded
fixture
demo
testData
fallback score
static response
example response

Examples:

const mockSkills = ...

const dummyRoadmap = ...

return { score: 75 }

fake interview feedback

hardcoded Android Developer roadmap

hardcoded Data Scientist questions

static chart history

placeholder AI response

Remove mock data from production code paths.

Mocks may remain only inside:

tests
test fixtures
Storybook/dev-only files

if clearly isolated.

---

# 7. REMOVE FAKE FALLBACK AI RESPONSES

Do NOT silently return fabricated analysis when the LLM request fails.

Bad:

try:
call_ai()
except:
return fake_analysis

Instead:

return a proper error state

or retry safely

or show:

"AI analysis is temporarily unavailable."

Never present mock/fabricated data as real AI analysis.

---

# 8. USE A REAL LLM FOR ALL AI FEATURES

Identify the actual LLM provider already configured in the project.

Use one shared LLM integration layer where practical.

Examples of AI-powered modules:

* resume analysis
* ATS analysis
* skill extraction
* skill-gap reasoning
* roadmap generation
* milestone generation
* study material
* AI mentor
* mock interview question generation
* mock interview evaluation
* career insight
* possibly cover letter assistance

All these should use the actual configured LLM/API.

Do not use static generated JSON in production.

---

# 9. SHARED LLM CLIENT

Create or reuse one canonical LLM client/service.

Example:

app/core/llm.py

or equivalent.

It should handle:

* provider initialization
* API key/config
* model selection
* timeout
* retry policy
* response parsing
* JSON mode/structured output where supported
* error handling
* logging

Do not initialize a different client independently inside every route.

---

# 10. ENVIRONMENT-BASED CONFIGURATION

LLM configuration must come from environment/config.

Examples:

LLM_PROVIDER

LLM_MODEL

LLM_API_KEY

LLM_TIMEOUT

Do not hardcode API keys.

Do not hardcode production secrets in frontend code.

LLM API calls should happen server-side.

---

# 11. STRUCTURED AI OUTPUTS

For machine-consumed AI features, use structured outputs.

Do not parse arbitrary prose with fragile regex if avoidable.

Use JSON/Pydantic/schema validation for:

Resume Analysis

Skill Gap

Roadmap

Milestones

Study Material

Interview Questions

Interview Evaluation

Career Insight

If LLM output fails validation:

retry once with correction where appropriate

or return a controlled error

Do not save malformed output.

---

# 12. RESUME ANALYSIS MUST BE REAL

Resume analysis must use the actual parsed resume content.

Never use:

filename
sample resume
previous candidate data
static content

The AI prompt should receive only the correct user's current resume.

Persist results linked to:

userId
resumeId

---

# 13. SKILL EXTRACTION MUST COME FROM THE ACTUAL RESUME

Detected skills must reflect the current resume.

Do not maintain a separate hardcoded skill list in the frontend.

Normalize skills centrally.

Examples:

js → JavaScript

nodejs → Node.js

postgres → PostgreSQL

reactjs → React

android sdk → Android SDK

Normalization should happen in backend/service logic, not inconsistently across multiple pages.

---

# 14. TARGET ROLE MUST BE SHARED GLOBALLY

The selected target role must be one persistent user/career preference.

Do not let different pages independently maintain:

Android Developer
iOS Developer
Data Scientist
Software Engineer

for the same active user unless intentionally switching.

Skill Gap, Roadmap, Mentor, Interview, and Overview should all read the same active target role.

When target role changes:

update it centrally.

---

# 15. SKILL GAP MUST USE CURRENT RESUME + CURRENT TARGET ROLE

Input to Skill Gap analysis should be:

current resume skills

*

current target role

Optionally:

experience/projects

The LLM/service should distinguish:

matched

transferable

other

missing/not detected

priority gaps

Do not compare against stale skill extraction.

Persist the analysis with references to the resume and target role used.

---

# 16. ROADMAP MUST COME FROM THE LATEST SKILL GAP

Roadmap generation should use:

* active target role
* current missing skills
* existing relevant skills
* user level
* prerequisites

Do not use static roadmap templates alone.

Templates can support the LLM, but final roadmap must be personalized.

Persist:

roadmapId
userId
targetRole
sourceSkillGapId / relevant source reference
generatedAt

---

# 17. ROADMAP MILESTONES MUST BE REAL AND PERSISTENT

Roadmap phases/milestones should come from the actual roadmap generation response.

Do not regenerate milestones every frontend refresh.

Persist:

phase
milestone
status
completed state
estimated time
skills
objectives

Progress should survive logout/login.

---

# 18. STUDY MATERIAL MUST USE THE CURRENT MILESTONE

When user generates study material:

Pass:

target role
phase
milestone
skills
learning objectives
existing skills
user level

Do not use generic static lessons.

Cache/persist generated material.

Do not call the LLM every time the page opens.

---

# 19. AI MENTOR MUST USE LIVE CAREEROS CONTEXT

Mentor context should be built server-side using the current user.

Include relevant, concise data:

active target role

latest resume analysis summary

detected skills

current skill gaps

roadmap current phase

current milestone

roadmap progress

latest interview feedback

portfolio summary

readiness score

Do not pass fake values.

Do not blindly pass huge raw database objects.

---

# 20. MENTOR MUST NOT CONFLICT WITH ROADMAP

Example:

If Kotlin is already detected in the resume and roadmap currently focuses on Android lifecycle:

Mentor should not say:

"Start by learning Kotlin basics."

It should use shared CareerOS state.

This is a core consistency requirement.

---

# 21. MOCK INTERVIEW QUESTIONS MUST USE REAL AI

Remove static/mock interview questions.

Generate questions using the actual LLM.

Input:

target role

resume skills

projects

experience

skill gaps

interview type

difficulty

question count

Do not invent candidate experience.

---

# 22. INTERVIEW EVALUATION MUST USE REAL AI

Remove static scoring or fake interview feedback.

The actual answer should be evaluated by the LLM.

Return structured:

overall score

technical knowledge

communication

problem solving

answer structure

role relevance

question-by-question feedback

strengths

areas to improve

Skipped questions must be explicitly marked as skipped.

---

# 23. CAREER READINESS MUST USE REAL CURRENT DATA

Audit the Career Readiness calculation.

Inputs may include:

ATS / resume score

skill-role match

interview score

portfolio score

roadmap/progress if intentionally included

Make sure each value is:

current

persisted

linked to the current user

not mock

Do not treat "not assessed" as zero unless that is an explicit product rule.

---

# 24. NO CROSS-USER DATA LEAKAGE

Audit every backend/AI query.

Every persisted record must be scoped to the authenticated user.

Never fetch:

latest resume globally

latest skill gap globally

latest roadmap globally

latest interview globally

It must always be:

latest for current authenticated user

This is critical.

---

# 25. AUTHENTICATED USER ID

Do not trust arbitrary userId values from frontend request bodies when the authenticated backend already knows the user.

Prefer identity from:

authenticated session/JWT/request context.

Prevent users from requesting another user's CareerOS data.

---

# 26. API RESPONSE CONSISTENCY

Audit frontend/backend response shape mismatches.

Examples:

snake_case vs camelCase

missingSkills vs missing_skills

targetRole vs target_role

roadmap.steps vs roadmap.phases

score vs atsScore

Create consistent contracts.

Do not silently support five incompatible response versions forever.

If backward compatibility is required, normalize at one boundary.

---

# 27. CENTRAL TYPES / SCHEMAS

Maintain canonical schemas.

Backend:

Pydantic / DTOs / validation

Frontend:

TypeScript interfaces/types

Ensure they match.

Remove duplicate DTO patch files.

Do not define slightly different `SkillGapResponse` interfaces on multiple pages.

---

# 28. FRONTEND DATA FETCHING

Audit all pages.

Overview

Resume

Skill Gap

Roadmap

AI Mentor

Mock Interview

Portfolio

Make sure each page fetches correct current data.

Avoid:

stale localStorage snapshots

duplicated static state

hardcoded sample data

unnecessary duplicate requests

---

# 29. CACHE INVALIDATION

If React Query / TanStack Query / SWR or similar is used:

use proper query keys.

Example:

["resume", userId]

["skill-gap", userId, resumeId, targetRole]

["roadmap", userId, roadmapId]

["interviews", userId]

After mutations:

invalidate the correct dependent queries.

Do not require full page refresh to see new data.

---

# 30. IF NO QUERY LIBRARY EXISTS

Do not install a major new state library just for this task unless clearly beneficial.

Fix existing data flow first.

Use existing architecture cleanly.

---

# 31. REFRESH / RE-ANALYZE BEHAVIOR

Buttons such as:

Refresh Score

Re-analyze

Generate Roadmap

Analyze Skills

must have clear semantics.

Do not silently trigger random dependent regeneration.

For example:

Re-analyze Skill Gap

should use:

latest resume
current target role

and then mark existing roadmap as possibly outdated.

---

# 32. STALE DATA UI

When data is stale, tell the user.

Example:

"Your skill-gap analysis was generated from an older resume."

[ Re-analyze ]

or

"Your target role changed after this roadmap was generated."

[ Update Roadmap ]

Do not quietly show inconsistent results.

---

# 33. TIMESTAMPS

Persist useful timestamps:

createdAt
updatedAt
generatedAt
completedAt

Use them to determine freshness.

Do not depend only on frontend current time.

---

# 34. DATABASE RELATIONSHIPS

Audit schema relationships.

Ensure correct ownership links exist.

Example conceptual relations:

User
→ Resumes

User
→ Target Career Profile

Resume
→ Resume Analysis

Resume Analysis / Career Profile
→ Skill Gap

Skill Gap
→ Roadmap

Roadmap
→ Phases
→ Milestones

User
→ Interviews
→ Interview Feedback

User
→ Mentor Conversations

Do not force this exact schema if existing schema can support equivalent relationships.

---

# 35. TRANSACTION / CONSISTENCY

Where multiple related records must be updated together, use database transactions if supported.

Example:

New resume upload:

create resume

save extracted content

save analysis

mark active resume

Avoid partially updated state.

---

# 36. ERROR HANDLING

Standardize API errors.

Frontend should receive useful controlled messages.

Examples:

AI_SERVICE_UNAVAILABLE

NO_ACTIVE_RESUME

SKILL_GAP_OUTDATED

ROADMAP_NOT_FOUND

INVALID_AI_RESPONSE

Do not expose stack traces.

Do not replace errors with fake data.

---

# 37. LOGGING

Add useful server-side logs for debugging data synchronization.

Examples:

userId

resumeId

skillGapId

roadmapId

interviewId

LLM request type

success/failure

Do not log:

full resumes unnecessarily

API keys

tokens

sensitive auth data

---

# 38. REAL AI FAILURE MODE

If LLM call fails:

1. retry according to shared policy where appropriate
2. return controlled error
3. keep previous valid persisted analysis if available
4. mark it as older/stale if appropriate

Do NOT replace it with mock AI output.

---

# 39. LLM PROMPT CONTEXT QUALITY

Each prompt should receive only relevant data.

Avoid huge duplicated prompts.

Examples:

Skill Gap:
resume skills + target role

Roadmap:
skill gaps + existing skills + target role

Mentor:
compact current state

Interview:
resume/projects + target role + gaps

Do not send the full resume to every AI request unless necessary.

---

# 40. MODEL RESPONSE VALIDATION

For every structured AI response:

validate enums

validate score ranges

validate required fields

validate arrays

validate malformed JSON

Scores must be bounded:

0–100

Do not accept invalid values and pass them directly to frontend.

---

# 41. REMOVE RANDOM SCORE LOGIC

Search for:

Math.random

random.randint

fake percentage

placeholder score

hardcoded 50/100

Remove such logic from production analysis.

All scores must come from:

deterministic calculation

or real LLM evaluation

with defined logic.

---

# 42. TEST THE FULL USER JOURNEY

After fixes, execute a complete end-to-end flow.

TEST FLOW:

1. create/use one test user
2. upload a resume
3. verify correct resume belongs to user
4. run resume analysis
5. verify real LLM response persisted
6. select target role
7. run skill-gap analysis
8. verify skill gap uses current resume
9. generate roadmap
10. verify roadmap uses latest skill gap
11. complete a milestone
12. refresh/re-login and verify persistence
13. generate study material
14. ask AI Mentor "What should I learn next?"
15. verify answer matches current roadmap
16. start mock interview
17. verify questions are personalized
18. submit answers
19. verify real AI feedback
20. refresh Career Readiness
21. verify Overview uses latest data

Document failures and fixes.

---

# 43. TEST DATA ISOLATION

Use explicit test fixtures only in testing.

Production code should never fall back to test-user data.

---

# 44. FRONTEND PLACEHOLDERS

Remove visible developer placeholders such as:

"chart goes here"

"mock feedback"

"sample roadmap"

"coming soon data"

If a feature lacks real data:

show a proper empty state.

---

# 45. DO NOT BREAK EXISTING UI IMPROVEMENTS

Preserve the redesigned UI already implemented for:

Skill Gap

Roadmap

Roadmap Milestones

Study Material

AI Mentor

Mock Interview

Overview

This task is mainly data correctness and real-AI integration.

Do not undo the UI unless necessary to fix broken data behavior.

---

# 46. DO NOT OVER-ENGINEER

Do not introduce unnecessary:

message queues

event sourcing

CQRS

multiple microservices

Kafka

Redis

complex distributed architecture

unless already present and needed.

CareerOS should remain understandable and maintainable.

---

# 47. REMOVE PATCH/HOTFIX IMPLEMENTATIONS

During the audit, also remove any remaining production source files such as:

fix_*.py

update_*.py

temp_*.py

after their logic has been safely consolidated.

Do not delete until verified.

---

# 48. FINAL EXPECTED ARCHITECTURE

There should be one canonical implementation per feature.

Examples:

Resume:
one canonical analysis service

Skill Gap:
one canonical service

Roadmap:
one canonical service

Study:
one canonical service

Mentor:
one canonical service

Interview:
one canonical generation/evaluation service

Readiness:
one canonical scoring service

No duplicate versions.

---

# 49. ACCEPTANCE CRITERIA

The task is complete only when:

* no production mock analysis remains
* no fake AI fallback remains
* no hardcoded AI scores remain
* resume analysis uses real current resume
* skill gap uses current resume + target role
* roadmap uses latest skill gap
* mentor uses current CareerOS context
* mock interview uses real LLM
* interview feedback uses real LLM
* readiness uses latest valid data
* new data appears without manual full-page refresh where possible
* user data is properly scoped
* old/stale data is detected
* the application starts successfully
* core flows work end-to-end

---

# 50. IMPLEMENTATION APPROACH

Follow this order:

PHASE 1
Audit / data flow map

PHASE 2
Remove production mock/fake sources

PHASE 3
Fix authenticated user scoping

PHASE 4
Fix resume → skill gap synchronization

PHASE 5
Fix skill gap → roadmap synchronization

PHASE 6
Fix roadmap → study/mentor context

PHASE 7
Replace mock interview generation/evaluation with real LLM

PHASE 8
Fix Career Readiness synchronization

PHASE 9
Frontend cache/state invalidation

PHASE 10
End-to-end verification

Do not make unrelated cosmetic changes during this task.

---

# FINAL REPORT

At the end, provide a detailed report containing:

1. Major bugs discovered

2. Root cause of each data synchronization issue

3. Mock/fake data removed

4. Hardcoded scores removed

5. Real LLM integrations added/fixed

6. Canonical source of truth for each module

7. Database/schema changes

8. API changes

9. Frontend state/cache changes

10. Stale data handling

11. User-scoping/security fixes

12. LLM failure behavior

13. Duplicate logic removed

14. Files changed

15. Files removed

16. End-to-end tests performed

17. Remaining known issues

18. Any migration required for existing CareerOS data

Do NOT claim the system is fully synchronized unless you actually verify the complete flow.
