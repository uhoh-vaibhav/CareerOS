I need you to fix a layout regression in the CareerOS Mock Interview page.

The left navigation sidebar has shifted to the right.

Previously, the sidebar was fixed/aligned directly under the top header on the far left side of the page.

Now there is a large empty gap between the left edge of the browser and the sidebar.

DO NOT redesign the sidebar.

DO NOT change the sidebar width, menu items, colors, active state, or navigation behavior.

Only fix its positioning so it returns to the same placement used on the other CareerOS pages.

## EXPECTED LAYOUT

The page should be structured like:

## Top Header

Sidebar            Main Content
|                  |
| Overview         | AI Mock Interview
| Resume           |
| Skill Gap        |
| Roadmap          |
| AI Mentor        |
| Mock Interview   |
| ...              |

The sidebar should begin from the left edge of the application layout, directly below the header.

There should NOT be a large blank margin before the sidebar.

## IMPORTANT

Inspect the shared layout used by working pages such as:

* Overview
* Resume
* Skill Gap
* Roadmap
* AI Mentor

The Mock Interview page should use the SAME sidebar/layout structure.

Do not create another sidebar component specifically for Mock Interview.

Reuse the existing shared CareerOS layout/sidebar.

## LIKELY ISSUE TO CHECK

Check whether the Mock Interview page or a parent wrapper was recently given something like:

max-width
margin: 0 auto
mx-auto
container
justify-center
grid centering

around BOTH the sidebar and main content.

The centered/max-width container should apply only to the MAIN CONTENT area, not to the overall application shell.

Incorrect pattern:

<div className="max-w-7xl mx-auto">
    <Sidebar />
    <MainContent />
</div>

This causes the sidebar to shift inward.

Use a structure like:

<div className="app-shell">
    <Sidebar />

```
<main className="main-content">
    <div className="content-container">
        ...
    </div>
</main>
```

</div>

The sidebar remains fixed/aligned left, while only the content inside main uses max-width.

## DESKTOP BEHAVIOR

Header:
full width

Sidebar:
fixed or sticky on the left depending on existing app behavior

Main content:
starts immediately after sidebar

For example:

sidebar width = existing width

main content:
margin-left or grid column equal to sidebar width

Then inside main:

max-width: 1400px
margin: 0 auto

The max-width must NOT affect the sidebar.

## DO NOT HARDCODE A RANDOM OFFSET

Do not fix this by adding something arbitrary like:

margin-left: -200px

or

left: -150px

Fix the actual layout hierarchy.

## CHECK THESE AREAS

Inspect:

1. shared AppLayout / DashboardLayout
2. Sidebar component
3. Mock Interview page wrapper
4. parent grid/flex containers
5. max-width classes
6. mx-auto / container classes
7. sidebar positioning
8. main content margin/padding
9. responsive breakpoints

## RESPONSIVE BEHAVIOR

Desktop:
sidebar stays on far left

Tablet/mobile:
preserve existing responsive/collapsible sidebar behavior

Do not break mobile navigation.

## FINAL REQUIREMENT

After the fix:

* Sidebar position must match Overview
* Sidebar position must match Resume
* Sidebar position must match Roadmap
* Mock Interview content can remain centered inside the main content area
* No large blank space should exist to the left of the sidebar

Do not redesign anything else.

At the end, tell me exactly which wrapper/class caused the shift and which files/classes you changed.
